# Benchmarks

Generated by `scripts/benchmark.py` (PLAN.md §10, WS-14, Phase 7). Every number on this page was measured on a real run of this script against a real `uvicorn` process in mock mode — none is invented (PLAN.md §10.3's honesty rule). Re-run the exact command below to reproduce.

## Environment

- **Date:** 2026-09-05T14:42:38+00:00
- **Commit SHA:** `a6139ded57298347300f6b9945f4bb1cbf599ce9`
- **Hardware:** Apple M2, 16 GiB RAM | macOS-26.6.2-arm64-arm-64bit | python 3.12.7
- **Command:** `python scripts/benchmark.py --iterations 1000 --warmup 100 --out docs/benchmarks.md`
- **Server:** `uvicorn app.main:app` (single worker, mock mode — no `UPSTREAM_BASE_URL`), `POLICY_PATH` pointed at a temporary copy of `policies/default_policy.yaml` with `rate_limit_default.requests` raised so the limiter does not shape the measurement (see docs/adr/0009).
- **Method:** 100-request warm-up (discarded) then 1000 measured sequential requests per shape, per PLAN.md §10.1.

## G1-G3: firewall-added latency (ms)

"Firewall-added latency" = wall-clock time inside the firewall excluding upstream time (PLAN.md §10.1). Budgets are aspirational (G1-G3's own `Nature` column), not pass/fail gates — recorded honestly below, whichever side of the budget they land on.

| Shape | Corpus case | Verdict (HTTP) | firewall_added source | n | mean | p50 | p95 | p99 | min | max |
|---|---|---|---|---:|---:|---:|---:|---:|---:|---:|
| benign_allow | BEN-001 | 200×1000 | server (firewall.timings_ms.firewall_added) | 1000 | 0.571 | 0.527 | 0.592 | 1.025 | 0.446 | 15.384 |
| deny | INJ-001 | 403×1000 | client (no upstream call made for this verdict) | 1000 | 2.449 | 2.376 | 2.567 | 3.376 | 2.193 | 17.464 |
| redact_allow | PII-001 | 200×1000 | server (firewall.timings_ms.firewall_added) | 1000 | 0.600 | 0.566 | 0.632 | 0.673 | 0.490 | 14.813 |
| need_approval | TOL-001 | 202×1000 | client (no upstream call made for this verdict) | 1000 | 4.924 | 4.775 | 5.512 | 8.368 | 4.148 | 20.541 |

- G1 budget: p50 ≤ 25 ms. G2 budget: p95 ≤ 60 ms. G3 budget: p99 ≤ 150 ms (PLAN.md §10.2, aspirational, carried over from PRD §7).

## G4: throughput, single worker, mock mode (req/s)

| Shape | Requests | Wall time (s) | Throughput (req/s) |
|---|---:|---:|---:|
| benign_allow | 1000 | 2.612 | 382.9 |
| deny | 1000 | 2.450 | 408.2 |
| redact_allow | 1000 | 3.014 | 331.8 |
| need_approval | 1000 | 4.925 | 203.1 |

Recorded, no threshold (PLAN.md §10.2 G4). Sequential single-client requests against a single `uvicorn` worker — a lower bound on real concurrent throughput, not an upper one; no attempt is made here to saturate the server with concurrent load.

## Raw client-observed request latency (ms), for reference

| Shape | n | mean | p50 | p95 | p99 | min | max |
|---|---:|---:|---:|---:|---:|---:|---:|
| benign_allow | 1000 | 2.597 | 2.509 | 2.728 | 4.576 | 2.333 | 17.785 |
| deny | 1000 | 2.449 | 2.376 | 2.567 | 3.376 | 2.193 | 17.464 |
| redact_allow | 1000 | 2.999 | 2.950 | 3.257 | 3.739 | 2.651 | 17.994 |
| need_approval | 1000 | 4.924 | 4.775 | 5.512 | 8.368 | 4.148 | 20.541 |

Includes full HTTP round-trip (localhost TCP + ASGI + handler), so these numbers are always ≥ the firewall-added figures above for the ALLOW shapes; for `deny` and `need_approval` the two tables are the same data (no upstream call is made either way).

## G12: upstream calls avoided (measured)

`realguard_upstream_calls_avoided_total` increased by **1100** during this run. The metric is currently only incremented on the `DENY` path (see app/main.py), and the `deny` shape issued 100 warm-up + 1000 measured = 1100 requests, so this figure equals that total exactly — not a broader count across every verdict, and not limited to the measured (post-warm-up) requests alone.

## G13: estimated cost per 1,000 transactions avoided (derived estimate)

**This is a derived arithmetic estimate, not an observed saving** (PLAN.md §10.3). It multiplies the measured `deny`-shape avoided-call count above by a stated public price: OpenAI gpt-4o-mini public API pricing, $0.15/1M input + $0.60/1M output tokens (checked via web search 2026-09-05; illustrative only, not re-verified at run time).

- Avoided-call count (measured, this run): **1100**
- Assumed prompt size per avoided call: **15 tokens** (this run's own `deny`-shape request text, using MockProvider's own `len(content) // 4` estimator — not the corpus's real average request size)
- Assumed completion size per avoided call: **0 tokens** (a `DENY` verdict never reaches a model, so there is no completion to price — this makes the estimate a lower bound, input-tokens-only)
- Price: $0.15/1M input tokens

Estimated cost avoided per 1,000 `DENY`-verdict transactions like this run's: **$0.0023** (input tokens only, at the cited price). This is a small, input-token-only illustration, not a claim about typical real-world request size or cost — see docs/adr/0009 for the full caveat.

## Notes and caveats

- Mock mode only. No live-model (Ollama) latency numbers are included in this file — real `qwen3:8b` inference latency is two to three orders of magnitude larger than firewall-added latency and would dominate any combined figure without adding information about the firewall's own overhead, which is what G1-G3 measure. Phase 5's Ollama profile and its own known latency variance are documented separately (docs/adr/0007, docs/adr/0008).
- All four request shapes are single JSON-Schema-simple payloads (one user message, short content). Larger payloads (e.g. BEN-012's ~2,000-word document) were not separately benchmarked this phase; detector cost scales with content length, so this is a real, named gap, not an oversight — see docs/adr/0009.
- The rate limiter is deliberately loosened for this benchmark run only (see Environment above) — these numbers do not include rate-limiter overhead.
